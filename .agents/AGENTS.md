# Workspace Rules

- **No Auto-Commits:** Do not ever automatically commit or push code. Always leave the changes staged or unstaged in the working directory for the user to review and commit manually, or ask for explicit permission before running any `git commit` or `git push` commands.
